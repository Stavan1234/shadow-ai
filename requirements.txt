# Core runtime (direct dependencies)
cognee==1.2.2
ollama==0.6.2
python-dotenv==1.2.2

# Optional — document ingestion (Phase C)
markitdown[ocr]==0.1.6

# Cognee pulls these transitively; pin for reproducible installs
lancedb==0.33.0
torch==2.12.1
transformers==5.12.1

# Phase 1 Voice I/O dependencies
faster-whisper
webrtcvad-wheels
pywebview
